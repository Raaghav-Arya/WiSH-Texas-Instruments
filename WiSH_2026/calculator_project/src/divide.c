#include <stdio.h>
#include "../inc/calculator.h"

/* TODO: implement division and return the result.
 *       Print an error and return 0.0f if b is 0. */
float divide(float a, float b)
{
    if(b != 0) {
        float c = a/b;
        return 0.0fc;    
    }
    return 0.0f;
}
